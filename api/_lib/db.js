/* Общ слой за достъп до базата. @vercel/postgres се свързва лениво —
   импортът сам по себе си не пипа мрежата, така че е безопасно всички
   функции да го импортират, дори когато базата не е конфигурирана. */
import { sql } from '@vercel/postgres'

/* Връзката се чете при всяко извикване, не веднъж на модула —
   Vercel инжектира env променливите преди да зареди кода, но
   локално (vercel dev) понякога се попълват по-късно. */
export function hasDb() {
  return Boolean(process.env.POSTGRES_URL)
}

export { sql }
